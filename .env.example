# ===== ReadBuddy Environment Variables =====

# --- App Environment ---
ENV=development
BACKEND_PORT=8000
FRONTEND_PORT=3000

# --- CORS (comma-separated) ---
ALLOWED_ORIGINS=http://localhost:3000,chrome-extension://*

# --- LLM Provider Settings ---
# Options: openai | anthropic | local
LLM_PROVIDER=openai

# OpenAI (if using LLM_PROVIDER=openai)
OPENAI_API_KEY=your_openai_api_key_here

# Anthropic (if using LLM_PROVIDER=anthropic)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# --- Vector Database (RAG) ---
# Options: pinecone | chroma
VECTOR_DB=chroma

# Pinecone (if using VECTOR_DB=pinecone)
PINECONE_API_KEY=your_pinecone_api_key_here
PINECONE_ENVIRONMENT=us-west4-gcp
PINECONE_INDEX_NAME=readbuddy

# --- Database (for analytics, optional in Week 2) ---
DATABASE_URL=postgresql://postgres:postgres@db:5432/readbuddy

# --- Logging ---
LOG_LEVEL=INFO

# --- Security (optional) ---
# Set a random, long string if you add auth later
JWT_SECRET=change_me_in_prod
JWT_EXPIRES_IN=7d
